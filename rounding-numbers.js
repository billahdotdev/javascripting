// rounding-numbers.js

// Define a variable named roundUp with the float 1.5
let roundUp = 1.5;

// Define a variable named rounded that stores the result of Math.round(roundUp)
let rounded = Math.round(roundUp);

// Print the rounded number to the terminal
console.log(rounded);
