// object-properties.js

// Define a variable named food that references an object with a types property
const food = {
  types: 'only pizza',
};

// Print the types property of the food object to the terminal
console.log(food.types);
