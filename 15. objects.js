// objects.js

// Define a variable named pizza that references an object with toppings, crust, and serves properties
const pizza = {
  toppings: ['cheese', 'sauce', 'pepperoni'],
  crust: 'deep dish',
  serves: 2,
};

// Print the pizza object to the terminal
console.log(pizza);
