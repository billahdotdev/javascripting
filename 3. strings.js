// strings.js

// Declare a constant variable someString and set it to 'this is a string'
const someString = 'this is a string';

// Use console.log() to print the someString variable to the terminal
console.log(someString);
