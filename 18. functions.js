// functions.js

// Define a function named eat that takes one argument named food (a string)
function eat(food) {
  // Return a string combining the food argument and a message
  return food + ' tasted really good.';
}

// Call the eat function with 'bananas' as the argument and print the result
console.log(eat('bananas'));
